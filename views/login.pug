extends layouts/login-layout.pug

block content
    .loginContainer
        h1 Login
        form(action="/login", method="post")

            p.errorMessage #{errorMessage}

            //- input(type="text", name="logUsername", placeholder="Username", value=logUsername ,required="")
            //- input(type="password", name="logPassword", placeholder="Password", required="")

            input(type="text", name="logUsername", placeholder="Username", value="vedant" ,required="")
            input(type="password", name="logPassword", placeholder="Password", value="Einstine", required="")
            input(type="submit", value="Login")
        span Need An Account?
            a(href="/register") Register